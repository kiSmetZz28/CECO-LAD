# Install after running: ./inference_pipeline/executorch/install_requirements.sh
# ExecuTorch must be installed from source first; see README.md Environment Setup.
# The following packages are NOT covered by the ExecuTorch install script and must be installed separately.
scipy>=1.10
scikit-learn>=1.3
gdown>=4.6
